You are an expert Java bug analyst. Your task is to perform an in-depth analysis of a failing test case to guide a sophisticated information retrieval process.
Based on the provided test code, test output, and the verdict from a preliminary verification stage, you must provide a structured analysis in JSON format. Your goal is to identify key areas for investigation and formulate concise search queries.

<TestCase>
${testCase}
</TestCase>

<Test Output>
${testOutput}
</Test Output>

<Initial Analysis Verdict>
${initialAnalysis}
</Initial Analysis Verdict>

Your analysis must address the following points:
1.  **Symptoms**: Concisely describe the core symptom of the bug.
2.  **Relevant Classes**: Identify the Java classes or APIs that might be involved in the failure.
3.  **Error Location**: Pinpoint the specific section of the test case code where the error most likely originates.
4.  **Core Keyword Queries**: Formulate queries for a web search engine using only the most essential keywords. The goal is to cast a wide net and get the maximum possible set of relevant results, which can be analyzed later.

    **Best Practices for Crafting Queries**:
    *   **Core Keywords Only**: Reduce the query to its absolute core components. For example, instead of "java.nio.channels.Selector internal locking mechanism", use "java Selector lock".
    *   **Combine Concepts**: Mix class names with core concepts like "concurrency", "memory", "thread safety".
    *   **Target Specifications**: When needed, use short terms like "JLS", "JVMS", or "RFC" alongside other keywords.

Please output your analysis results in strict JSON format. Do not change the field keys:
{
  "symptoms": "Brief description of the Bug symptoms",
  "relevantClasses": ["java.net.Socket", "javax.net.ssl.SSLSocket"],
  "errorLocation": "Code section where the error might occur",
  "queries": ["java write stream after close", "RFC2965", "JLS float narrowing"]
} 