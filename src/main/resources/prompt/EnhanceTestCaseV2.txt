<Tasks>
Your primary goal is to enhance the provided test case by adding meaningful and non-redundant assertions. Follow this structured process:

1. **Analyze and Understand**:
   - **Review the Test Case**: Carefully examine the existing test code (`<TestCase>`) to understand what is already being tested.
   - **Study the API**: Read the provided API documentation (`<API Documentation>`) to grasp the core functionality, expected behavior, parameter constraints, and relationships between different methods/classes.
   - **Identify Gaps**: Compare the existing test against the API documentation to find untested features, interactions, or edge cases.

2. **Develop New Test Scenarios**:
   - **Think Creatively**: Go beyond simple use cases. Consider potential vulnerabilities, misuse scenarios, and complex interactions that a senior test engineer would look for.
   - **Focus on Interactions**: Design tests that verify how the API interacts with other standard Java components (e.g., Collections, Serialization, `equals`/`hashCode`).
   - **Cover Edge Cases**: Add tests for boundary values, null inputs, el characters, and other non-standard conditions as described in the API documentation.
   - **Consider Advanced Scenarios**:
     * **Performance**: Test with large datasets or complex inputs to check for performance bottlenecks or excessive algorithm complexity.
     * **Resource Safety**: Verify correct resource management to prevent potential memory or resource leaks (e.g., unclosed streams).
     * **Numerical Precision**: For numerical APIs, test for potential precision loss with known edge-case values.
     * **Security**: If the API handles external input (e.g., files, data streams), consider potential vulnerabilities.

   
3. **Implement and Document Enhancements**:
   - **Add Assertions**: Write clear, precise, and deterministic assertions that verify the intended behavior.
   - **Mark Your Changes**: Prepend every new assertion or test block with the comment `// Enhanced Test Oracle by LLM`.
   - **Provide Rationale**: Immediately following the marker comment, add another comment explaining *why* the new assertion is valuable and what specific scenario it covers.
   - **Write Rich Diagnostics**: Include descriptive error messages in assertions to provide clear information on failure, you can also print curial variables or states if necessary.
   - **Maintain Code Quality**: Ensure the final test case is complete, free of compilation errors, and follows the existing code style.
</Tasks>

<Requirements>
1. **Strict Adherence to Documentation**: Base all assertions *strictly* on the official API documentation (JavaDoc). Do not assert on implementation-specific details or undocumented behavior. For ambiguous cases, test for reasonable outcomes rather than specific results, and add comments explaining the rationale.

2. **Code Integrity**: Preserve the existing test framework structure (`jtreg` comments, imports, etc.). All new code must be syntactically correct, free of compilation errors, and deterministic.

3. **Output Format**: You must follow the specified output format. The final output should be a single, complete Java code block containing your thoughts and the final test case.
</Requirements>

<Output Format>
Your Thoughts here.
```java
TestCase Code Here.
```
</Output Format>

<TestCase>
${testcase}
</TestCase>

<API Documentation>
${apiDocs}
</API Documentation>
