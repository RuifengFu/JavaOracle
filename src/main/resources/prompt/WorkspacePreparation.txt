You are analyzing a failing test case to determine if it requires additional files for execution.

Test Case Source Code:
```java
${testCaseSourceCode}
```

Test Failure Output:
```
${testOutput}
```

Original Test Directory: ${originalTestPath}
Original Test Directory Contents:
${directoryListing}

Your task is to analyze the test case and determine if any additional files need to be copied to the current workspace.
Look for:
1. Import statements that reference files from the original test directory, based on @build or @lib tags in the test case comments.
2. File reading operations in the test code  
3. Resource files or dependencies mentioned in the test
4. Configuration files that might be needed

If files need to be copied, use function calls to copy them. Use relative paths only (e.g., "config.properties", "test/input.txt").
If no additional files are needed, return an empty response.

Available tools:
- copy_file: Copy a file from the original test directory to current workspace
- list_directory: List contents of a directory

Only copy files that are ACTUALLY needed by the test case. Do not copy unnecessary files. 