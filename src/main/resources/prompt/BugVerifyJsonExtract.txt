Extract the unique, complete JSON object or array from the following text.
Strictly return only the extracted JSON content, without including any explanations, code markers (e.g., ```json ```), or other text.
Ensure the output is a syntactically complete and correct JSON structure.
Must directly output the extracted JSON content in valid JSON format:

${input} 